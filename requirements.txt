# ============================================================
# Zepto AI Review Engine — Python Dependencies
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ============================================================

# ── Core ────────────────────────────────────────────────────
python-dotenv>=1.0.0          # Load .env into os.environ
pydantic>=2.6.0               # Data validation & schema enforcement
rich>=13.7.0                  # Pretty terminal output / progress bars
tqdm>=4.66.0                  # Progress bars for batch operations

# ── Data Ingestion ────────────────────────────────────────────────
google-play-scraper>=1.2.4    # Play Store reviews (no API key required)
app-store-scraper>=0.3.5      # Apple App Store reviews
requests>=2.23.0              # HTTP requests — used for public scraping and API calls
beautifulsoup4>=4.12.0        # HTML parsing for forum scrapers
lxml>=5.1.0                   # Fast XML/HTML parser (bs4 backend)
httpx>=0.27.0                 # Async HTTP client for concurrent scraping

# ── Data Storage ─────────────────────────────────────────────
# SQLite is part of Python stdlib — no install needed

# ── Preprocessing ────────────────────────────────────────────
langdetect>=1.0.9             # Language detection (English vs. Hindi vs. Hinglish)
datasketch>=1.6.4             # MinHash LSH for near-duplicate detection
nltk>=3.8.1                   # Tokenization utilities
tiktoken>=0.6.0               # OpenAI tokenizer — used for chunk sizing

# ── Embedding & Vector DB ────────────────────────────────────
# Embedding: sentence-transformers runs locally — zero API cost
sentence-transformers>=2.7.0  # Local embedding model (all-MiniLM-L6-v2)
torch>=2.2.0                  # Required backend for sentence-transformers
chromadb>=0.5.0               # Local vector DB for development (free, no cloud)
langchain>=0.2.0              # LLM + retriever orchestration
langchain-google-genai>=1.0.3 # LangChain Gemini integration (free tier)
langchain-community>=0.2.0    # LangChain ChromaDB integration

# ── Theme Identification (Clustering) ────────────────────────
umap-learn>=0.5.6             # Dimensionality reduction (UMAP)
hdbscan>=0.8.33               # Density-based clustering (no K required)
scikit-learn>=1.4.0           # Metrics, preprocessing utilities
numpy>=1.26.0                 # Numerical computing
pandas>=2.2.0                 # Tabular data handling
matplotlib>=3.8.0             # Cluster visualization plots
seaborn>=0.13.0               # Statistical data visualization

# ── LLM (Google Gemini — free tier via Google AI Studio) ────
# Get a free API key at: https://aistudio.google.com/app/apikey
# Free tier: Gemini 1.5 Flash — 15 RPM, 1 million tokens/day
google-generativeai>=0.7.0    # Google Gemini Python SDK

# ── Validation (RAGAS) ───────────────────────────────────────
# RAGAS is configured to use Gemini as its judge LLM (free tier).
ragas>=0.1.9                  # RAG faithfulness + coverage evaluation

# ── Output & Reporting ───────────────────────────────────────
flask>=3.0.0                  # Web dashboard for visual analysis results
jinja2>=3.1.3                 # HTML templating

# ── Development & Testing ────────────────────────────────────
pytest>=8.1.0                 # Unit testing
pytest-asyncio>=0.23.0        # Async test support
black>=24.3.0                 # Code formatter
ruff>=0.3.0                   # Fast Python linter
ipykernel>=6.29.0             # Jupyter kernel (for exploratory notebooks)
jupyter>=1.0.0                # Notebook support
